[workspace]
resolver = "2"
members = [
    "tax-web",
    "tax-cli",
    "tax-core"
]

[package]
name = "tax_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
tax-cli = { version = "0.1.0", path = "tax-cli" }
tax-core = { version = "0.1.0", path = "tax-core" }
